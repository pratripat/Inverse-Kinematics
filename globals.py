import pygame

#Initialize screen
width = height = 600
screen = pygame.display.set_mode((width, height))
pygame.display.set_caption('Inverse Kinematics')

#Colors
black = (0,0,0)
white = (255,255,255)
